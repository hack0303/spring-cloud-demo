server:
  port: @service.client.b.port@
  #servlet:
   # context-path: /product
spring:
  application.name: @application.name@
  profiles.active: @profiles.active@

################ Eureka ################
eureka:
  client:
    service-url:
      defaultZone: @eureka.client.service-url.defaultZone@